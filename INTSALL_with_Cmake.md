# Install With Cmake

```bash 
$ git clone https://github.com/long-gong/forked-minisketch.git
$ cd forked-minisketch
$ mkdir build && cd build
$ cmake ..
$ make
$ [sudo] make install
```

## Install with `vcpkg`

